{
  "name": "ml",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ./tools/build.mjs",
    "build:watch": "node ./tools/build.mjs --watch",
    "build:opt": "node ./tools/build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
